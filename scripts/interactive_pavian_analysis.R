#For this script you need to download your centrifuge reports.tsv
#To the directory you set below:

setwd("~/isogenie-permafrost/dna/cfuge")

# options(repos = c(CRAN = "http://cran.rstudio.com"))
# if (!require(remotes)) { install.packages("remotes") }
# remotes::install_github("fbreitwieser/pavian")

library(Rsamtools)

